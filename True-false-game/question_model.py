# Question class will set up initial attributes (text and the answer) via constructor

class Question:

    # constructor
    def __init__(self, text, answer):
        self.text = text
        self.answer = answer
